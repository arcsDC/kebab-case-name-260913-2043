# kebab-case-name

one concise sentence

## Stack

Primary language: **exact target language**

## Project structure

- `appropriate primary filename for that language/platform` — concise purpose
- `requirements.txt` — dependencies
- `tests/test_main.py` — basic tests

## Usage

Review the source files and install any dependencies referenced by the project before running it.
